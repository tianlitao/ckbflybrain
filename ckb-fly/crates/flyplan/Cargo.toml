[package]
name = "flyplan"
description = "Host-side planner: turns fly states and actions into the bytes a transaction needs"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "flyplan"
path = "src/main.rs"

[dependencies]
flycircuit = { path = "../flycircuit" }
flycore = { path = "../flycore" }
